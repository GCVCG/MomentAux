# diag: ImageNet init breaks the data contract.
# E3, referee-requested: does the transfer tax survive a weaker or
# delayed prior? Champion arm is weight 1.0 from step 0.
name: diagtaxlam_c10_l005_5pct
dataset: cifar10
backbone: resnet18
epochs: 200
batch_size: 128
lr: 0.1
weight_decay: 0.0005
momentum: 0.9
num_workers: 2
small_input: true
pretrained: true
subset_pct: 5
stem: none
stem_calibrate: true
moment_aux:
  stem: energy-magnitude
  tap: layer3
  weight: 0.05
  weight_final: 0.0
  weight_schedule: cosine
  head_norm: true
