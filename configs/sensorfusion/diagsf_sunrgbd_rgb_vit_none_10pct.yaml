# SUN RGB-D sensor fusion on ViT-tiny (64px => patch 8, 8x8 tokens, tap
# blocks.8) -- is the source-level result specific to ResNet-18? AdamW =>
# diagnostic namespace. SOURCE A: RGB appearance, 3 channels.
name: diagsf_sunrgbd_rgb_vit_none_10pct
dataset: sunrgbd_rgb
backbone: vit_tiny
optimizer: adamw
epochs: 200
batch_size: 128
lr: 0.001
weight_decay: 0.05
momentum: 0.9
num_workers: 2
small_input: true
pretrained: false
subset_pct: 10
stem: none
stem_calibrate: true
